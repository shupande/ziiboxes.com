---
import BaseLayout from "../../layouts/BaseLayout.astro";
import QuoteSection from "../../components/QuoteSection.astro";
import { products } from "../../data/site";

const productCount = products.length;
---

<BaseLayout
  title={`${productCount}+ Custom Paper Box Products`}
  description={`Explore ${productCount}+ custom paper box styles for ecommerce, retail, gift, wine, spirits, tea, chocolate, electronics, supplement, pet, stationery, toy and Amazon packaging projects.`}
  path="/products/"
  solidHeader
>
  <main>
    <section class="page-hero">
      <div class="wrap">
        <p class="eyebrow">Products</p>
        <h1>{productCount}+ custom paper box styles for online, retail and gift packaging.</h1>
        <p>Start with the product use case, then compare box structure, material, size, printing, finish, sample and shipping.</p>
      </div>
    </section>
    <section class="section alt">
      <div class="wrap product-grid">
        {products.map((product) => (
          <a class="product-card" href={`/products/${product.slug}/`}>
            <img src={product.image} alt={product.alt} />
            <div class="product-copy">
              <small>{product.keyword}</small>
              <h3>{product.title}</h3>
              <p>{product.description}</p>
            </div>
          </a>
        ))}
      </div>
    </section>
    <QuoteSection />
  </main>
</BaseLayout>
